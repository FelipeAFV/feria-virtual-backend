import { Router } from "express";
import passport from "passport";

const router: Router = Router();

// router.get('/signIn', passport.use('jwt'), authController.signIn);
// router.get('/signUp', authController.signUp);

export default router;